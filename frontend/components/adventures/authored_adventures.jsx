var React = require('react');

var AuthoredAdventures = React.createClass({
  render: function() {
    return (
      <div>
        authored
      </div>
    );
  }
});

module.exports = AuthoredAdventures;
